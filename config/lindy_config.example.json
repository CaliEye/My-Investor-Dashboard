{
  "webhook_url": "https://public.lindy.ai/api/v1/webhooks/lindy/YOUR_WEBHOOK_ID_HERE",
  "notes": {
    "setup": "Copy this file to lindy_config.json (gitignored) and paste your real Lindy webhook URL.",
    "proxy": "For extra security, deploy a Cloudflare Worker proxy and point webhook_url at your worker instead of the direct Lindy URL."
  }
}
